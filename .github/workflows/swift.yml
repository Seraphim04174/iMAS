name: Build iOS App (KMM no CocoaPods)

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17 (Kotlin)
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Set up Gradle
        uses: gradle/gradle-build-action@v2

      - name: Build shared KMM module
        run: ./gradlew :shared:build

      - name: Debug Xcode schemes
        run: xcodebuild -list -project iosApp/iosApp.xcodeproj

      # После выполнения предыдущего шага, зайди в GitHub Actions и посмотри название схемы.
      # Например, если в выводе Debug Xcode schemes будет "iOSApp", то укажи это в -scheme ниже.
      - name: Build iOS app with xcodebuild
        run: |
          xcodebuild clean build \
            -project iosApp/iosApp.xcodeproj \
            -scheme iOSApp \  # <- замените 'iOSApp' на фактическое имя схемы
            -sdk iphonesimulator \
            -destination 'platform=iOS Simulator,name=iPhone 14'
